{block header} Správa uživatelů{/block} 
{block content}
    <div class="row">
        <div class="col-sm-12">
            {ifset $allUsers}
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Email</th>
                        <th>Křestní jméno</th>
                        <th>Příjmení</th>
                        <th>Telefonní číslo</th>
                        <th>Úroveň</th>
                        <th>Správa</th>
                    </tr>
                </thead>
                <tbody>

                    {foreach $allUsers as $row}
                    <tr>
                        <th><a n:href="Admin:editUser $row->id_user">{$row->id_user}</a></th>
                        <td>{$row->email}</td>
                        <td>{$row->first_name}</td>
                        <td>{$row->surname}</td>
                        <td>{$row->phone}</td>
                        <td>{if $row->rank==1}Student{elseif $user->rank==2}Lektor{elseif $user->rank==3}Garant{elseif $user->rank==4}Vedoucí{elseif $user->rank==5}Administrátor{/if}</td>
                    </tr>
                    {/foreach}

                </tbody>
            </table>
            {else}
            <h3>Žádní uživatelé nejsou k dispozici!</h3>
            {/ifset}
        </div>
    </div>

{/block}